pull_request_rules:
  - name: VM6 same-SHA own-push provenance
    conditions:
      - title = VM6 same-SHA own-push provenance 20260917
    actions:
      dismiss_reviews:
        approved: true
        changes_requested: false
merge_protections:
  - name: VM6 CrossRepo Strict
    if: []
    success_conditions:
      - label = vm6-crossrepo-required
merge_protections_settings:
  reporting_method: check-runs
  post_comment: true
